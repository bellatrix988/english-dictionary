import { BlackListDirective } from './black-list.directive';

describe('BlackListDirective', () => {
  it('should create an instance', () => {
    const directive = new BlackListDirective();
    expect(directive).toBeTruthy();
  });
});
